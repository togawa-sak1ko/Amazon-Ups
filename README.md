# UPS + Amazon C Integration

Combined integration repo for:

- Mini-UPS in `erss-final-project-tl396-ldw59-hs452`
- team C Mini-Amazon in `erss-project-zx158-zq65`
- world simulator in `world_simulator_exec`

Start the full local stack from the repo root:

```bash
./start_docker_only.sh
```

More details are in [startup_instructions.md](startup_instructions.md).
